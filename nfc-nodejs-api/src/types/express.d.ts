// import { DecodedToken } from '../middleware/authMiddleware'; // Adjust the path as per your project structure

// declare global {
//   namespace Express {
//     interface Request {
//       user?: DecodedToken; // Define the user property to hold the decoded token
//     }
//   }
// }
